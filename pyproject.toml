[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[project]
name = "store-intelligence"
version = "1.0.0"
description = "Real-time retail analytics system powered by CCTV-based computer vision"
requires-python = ">=3.11"
